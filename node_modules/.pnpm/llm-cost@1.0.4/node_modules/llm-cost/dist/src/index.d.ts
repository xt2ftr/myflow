export declare function tokenizeAndEstimateCost({ model, input, output, }: {
    model: string;
    input?: string;
    output?: string;
}): Promise<{
    inputTokens: number;
    outputTokens: number;
    cost: number | undefined;
}>;
export declare function estimateCost({ model, inputTokens, outputTokens, }: {
    model: string;
    inputTokens?: number;
    outputTokens?: number;
}): number | undefined;
